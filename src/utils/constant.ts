/* eslint-disable no-unused-vars */

export enum STATUS {
  PENDING = 'pending',
  REQUEST_UPDATE = 'request update',
  APPROVED = 'approved'
}

export enum ROLE {
  STUDENT = 'student',
  CENTER = 'center',
  ADMIN = 'admin'
}
